# gcloud run regions

view available Cloud Run (fully managed) regions

### `gcloud run regions list`

List available Cloud Run (fully managed) regions

List available Cloud Run (fully managed) regions.

**Synopsis:**
```
gcloud run regions list [--filter=EXPRESSION] [--limit=LIMIT]
    [--page-size=PAGE_SIZE] [--sort-by=[FIELD,...]] [--uri]
    [GCLOUD_WIDE_FLAG ...]
```

**Examples:**
```bash
To list available Cloud Run (fully managed) regions:

    $ gcloud run regions list
```

[Official reference](https://cloud.google.com/sdk/gcloud/reference/run/regions/list)

---